import AddtoCart from "./AddToCart";

const ProductCard: React.FC = () => {
  return (
    <div>
      {/* The component below is a client component that can interact with the user and andles client-side logic */}
      <AddtoCart />
    </div>
  );
}

export default ProductCard;
